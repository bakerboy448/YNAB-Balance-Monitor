# Required
YNAB_API_TOKEN=your-ynab-personal-access-token
YNAB_ACCOUNT_ID=your-checking-account-id

# Optional — defaults shown
# YNAB_BUDGET_ID=last-used
# YNAB_CC_CATEGORIES=            # comma-separated category IDs or names; empty = all
# MONITOR_DAYS=                  # empty = end of current month; set to number of days to override
# MIN_BALANCE=0                  # alert threshold in dollars

# Check schedule — empty = run once and exit
# SCHEDULE=08:00                 # daily at 8am (uses TZ)
# SCHEDULE=6h                   # every 6 hours
# TZ=America/New_York           # timezone for daily schedule

# Update notification schedule — sends the projected minimum balance on a routine cadence
# Uses the same format as SCHEDULE; independent of SCHEDULE (can differ)
# UPDATE_SCHEDULE=18:00         # daily summary at 6pm
# UPDATE_SCHEDULE=12h           # every 12 hours

# Notifications — choose Notifiarr (rich Discord embeds) or Apprise (plain text fallback)
# At least one of NOTIFIARR_API_KEY or APPRISE_URLS is required.

# Notifiarr passthrough (preferred — color-coded Discord embeds with inline fields)
# Get your API key from https://notifiarr.com — Passthrough integration
# NOTIFIARR_API_KEY=your-notifiarr-api-key
# NOTIFIARR_CHANNEL_ID=123456789012345678       # Discord channel ID for alerts
# NOTIFIARR_UPDATE_CHANNEL_ID=                   # optional: separate channel for routine updates (defaults to NOTIFIARR_CHANNEL_ID)

# Apprise fallback (plain text — supports 100+ services)
# Comma-separated Apprise URLs. See https://github.com/caronc/apprise/wiki
# Examples:
#   ntfy://ntfy.sh/your-topic
#   tgram://bot-token/chat-id
#   slack://token-a/token-b/token-c/#channel
#   discord://webhook-id/webhook-token
APPRISE_URLS=ntfy://ntfy.sh/your-topic

# Optional: separate Apprise URLs for routine update notifications.
# Defaults to APPRISE_URLS if not set.
# UPDATE_APPRISE_URLS=ntfy://ntfy.sh/your-low-priority-topic
